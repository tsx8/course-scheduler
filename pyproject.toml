[project]
name = "course-scheduler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ortools>=9.14.6206",
    "pyinstaller>=6.14.1",
]

[tool.ty.environment]
python = ".venv"